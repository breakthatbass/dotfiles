#!/bin/bash
#
# use fzf to search through zsh history and execute a command
#
# usage: hzf

# history is a built in command and can't be called 
HISTFILE=$HOME/.bash_history
set -o history

d=$(history | awk '{$1=""; print}' | fzf)

if [[ "$d" == '' ]]; then
  exit 0
else
  eval "$d"
fi 
