#!/bin/bash

if [[ "$OSTYPE" == "darwin"* ]];
then
  ls /dev/cu.usb*
  echo
  ls /dev/tty.usb*
elif [[ "$OSTYPE" == "linux-gnu"* ]];
then
  ls /dev/ttyACMO
  echo
  ls /dev/serial/by-id
  echo
  ls /dev/serial/by-path
else
  echo "not supported"
fi
